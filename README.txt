PROBLEM:
1) приложение не работает больше 7ми часов, не удаляется процесс предыдущего драйвера перед создание нового (RAM в актуальном состоянии?) - цель - min 24h - COMPLETE!
2) если нашли событие то пушим в телеграм до усрачки, ввести кеш событий - O
3) некоторые события не успевают распарсится так как не в DOM уже (разбить по категориям - вид спорта), подробный разбор см. ниже CRITICAL
4) вопрос живости бота (пока - пушить в отдельный канал) - R
5) логировать только важную инфу - R

Nice to have:
1) все в докере
2) общаться с ботом через телеграм (nginx + webhook)
3) сделать все демоном

appendix:
1) чекать процессы после удаления драйвера (bash скрипт)	COMPLETE
2) если из п.1 есть процесс/процессы - килить их (bash скрипт)	COMPLETE

	Главные задачи.
1) За последнюю неделю бот нашел только одно событие, необходим алгоритм принятия решения действительно ли событий не было или мы их упускаем
	- как минимум, поможет градация событий на виды спорта, добавить вид спорта в Event insatnce
2) Разбивка на категории:
	- вверху Live окна есть кнопочки выбора спорта реализовать алгоритм:
		- выключаем все галочки
		- включаем следующую
	- для каждой галочки собирать события с указанием вида спорта и сравнения проводить только по ним:
		- модифицировать метод get_data, он должен принимать вид спорта который будет искать
решение этого - уменьшит колличество не распаршенных событий, позволит проще анализировать работу, улучшит жизнь и читаемость!
