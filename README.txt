1) если parse_to_events завершился с ошибкой - вернуть false и выкинуть этот webelement из сета uniq, по крайней мере если снова считаем этот эллемент, то снова попробуем его распарсить
2) собирать статистику от каждого прогона get_data и логировать, формат: считано различных эллементов(не однозначно из-за п.1), кол-во созданных Events, кол-во ошибок парсинга(тоже не однозначно из-за п.1)
3) добавить логирование перед и после создания и удаления драйвера
4) чекать процессы после удаления драйвера (bash скрипт)
5) если из п.4 есть процесс/процессы - килить их (bash скрипт)
6) сделать все демоном
